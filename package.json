{
  "name": "workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bundle exec jekyll build",
    "deploy": "wrangler deploy",
    "deploy:full": "bundle exec jekyll build && wrangler deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ethers": "^5.8.0",
    "express": "^5.2.1",
    "pg": "^8.20.0",
    "serve": "^14.2.6",
    "wrangler": "^4.84.0"
  }
}
